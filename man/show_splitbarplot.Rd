% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/show_splitbarplot.R
\name{show_splitbarplot}
\alias{show_splitbarplot}
\title{Show a Split Bar Plot with Error Bars}
\usage{
show_splitbarplot(
  df,
  period_col,
  year_col,
  value_col,
  x_label_prefix = "",
  interactive = TRUE,
  source = "A",
  exploded = FALSE,
  bars_fill = c("#00BFC4", "#F8766D"),
  bars_alpha = 0.7,
  label_points = c("min", "max", "median"),
  label_color = "black",
  label_template = "{year}: {value} ({type})",
  hover_template = "{year}: {value}",
  value_round = 2,
  text_size = 14
)
}
\arguments{
\item{df}{A data frame containing the data to plot}

\item{period_col}{Name of the column containing values "before" and "after" as an ordered factor}

\item{year_col}{Name of the column containing year values}

\item{value_col}{Name of the column containing the values to plot}

\item{x_label_prefix}{Optional prefix for x-axis labels (default: "")}

\item{interactive}{Logical; if TRUE, creates an interactive plot using plotly (default: TRUE)}

\item{source}{if interactive is TRUE, the \code{source} argument to \code{plotly::ggplotly} for referencing with click events (default: "A")}

\item{exploded}{Logical; if TRUE, adds individual points with labels for min, max, and mean (default: FALSE)}

\item{bars_fill}{Fill color for bars (default: \code{c("#00BFC4", "#F8766D")}; teal and red)}

\item{bars_alpha}{Alpha transparency for bars (default: 0.7)}

\item{label_points}{Character vector specifying which points to label in exploded view (default: c("min","max","median"))}

\item{label_color}{Color for labeled points in exploded view (default: "black")}

\item{label_template}{Glue expression for point labels (default: "{year}: {value} ({type})")}

\item{hover_template}{Glue expression for hover text (default: "{year}: {value}")}

\item{value_round}{Integer indicating number of decimal places for rounding values (default: 2)}

\item{text_size}{Size of text for labels and hover text (default: 14)}
}
\value{
A ggplot2 object or plotly object (if interactive = TRUE)
}
\description{
This function creates a bar plot with error bars based on two groups split by a year value.
It can optionally create an interactive plot and show individual data points.
}
\examples{
# Create example data
df <- data.frame(
  year = 2010:2020,
  value = rnorm(11, mean = 10, sd = 2)
)

# Basic interactive plot
show_splitbarplot(df, "year", "value", 2015)

# Custom colors and labels with 1 decimal place
show_splitbarplot(df, "year", "value", 2015,
            bar_fill = "steelblue",
            label_points = c("min", "max"),
            label_color = "darkred",
            value_round = 1)

# Custom label templates
show_splitbarplot(df, "year", "value", 2015,
            label_template = "{year} ({type})",
            hover_template = "Year {year}",
            value_round = 0)
}
\concept{show}
