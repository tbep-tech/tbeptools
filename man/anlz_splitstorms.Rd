% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/anlz_splitstorms.R
\name{anlz_splitstorms}
\alias{anlz_splitstorms}
\title{Analyze storm data split by date within years}
\usage{
anlz_splitstorms(
  df_hurricane,
  date_split,
  stats = list(sum = sum, avg = mean, n = length)
)
}
\arguments{
\item{df_hurricane}{data frame containing date_beg and scale columns for hurricanes}

\item{date_split}{date to split analysis into annual periods}

\item{stats}{list of functions to apply to scale values, default: sum, average and count.
Functions should accept numeric vector input and return single value}
}
\value{
A tibble summarizing hurricanes for annual periods "before" and "after" the split date,
with requested statistics computed for the scale values
}
\description{
Analyze storm data split by date within years
}
\examples{
# Create sample hurricane data
hurricanes <- data.frame(
  date_beg = as.Date(c(
    "1980-07-31", "1980-09-04", "1980-11-07",
    "1981-05-06", "1981-08-07", "1981-11-12")),
  scale = c(6, 1, 3, 1, 2, 1))

# Basic analysis with default statistics (sum, average and count)
split_date <- Sys.Date() - years(1)
anlz_splitstorms(hurricanes, split_date)

# Analysis with custom statistics
anlz_splitstorms(hurricanes, split_date,
                     stats = list(
                       max = max,
                       min = min))
}
\concept{analyze}
